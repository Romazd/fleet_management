<div class="container">
  <div class="header">
    <h1><%= t('maintenance_services.new.title') %></h1>
  </div>

  <div class="card">
    <div class="card-body">
      <h3>Vehículo:<%= @vehicle.brand %> <%= @vehicle.model %> (<%= @vehicle.year %>)</h3>
      <p>VIN: <%= @vehicle.vin %> | Placa: <%= @vehicle.plate %></p>
      <hr>
      <%= render 'form', maintenance_service: @maintenance_service %>
    </div>
  </div>
</div>