<div class="container">
  <div class="header">
    <h1><%= t('vehicles.new.title') %></h1>
  </div>

  <div class="card">
    <div class="card-body">
      <%= render 'form', vehicle: @vehicle %>
    </div>
  </div>
</div>